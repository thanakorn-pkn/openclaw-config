# 2026-03-27

## Gemini CLI Setup (morning session)

### Problem
Gemini CLI was using OAuth (`cloudcode-pa.googleapis.com` Code Assist endpoint) which consistently returned 429 MODEL_CAPACITY_EXHAUSTED for all models.

### Solution journey
1. Tried `selectedType: "gemini-api-key"` in `~/.gemini/settings.json` — correct value (discovered via source code: `AuthType.USE_GEMINI = "gemini-api-key"`)
2. Removed OAuth creds: `~/.gemini/oauth_creds.json` → `.bak`, `~/.gemini/google_accounts.json` → `.bak`
3. API key from 1Password via service account token — works

### 1Password Setup (headless server)
- **No desktop app** on nuc-13-pro → `op signin` requires interactive terminal
- **Service account token** is the correct approach for headless servers
- Token stored at: `~/.config/secrets/secrets.env` (chmod 600)
- Directory: `~/.config/secrets/` (chmod 700) with README.md
- `~/.bashrc` sources `~/.config/secrets/secrets.env` silently
- `op` usage: must pass `OP_SERVICE_ACCOUNT_TOKEN` explicitly: `OP_SERVICE_ACCOUNT_TOKEN="$OP_SERVICE_ACCOUNT_TOKEN" op read "..."`

### 1Password item details
- Vault: **Homelab**
- Item: `Gemini API Key (AI Studio)`
- Item ID: `2gjnghsjrrckf32qaftg3q2wdu`
- Field: `credential`
- Note: parentheses in item name are invalid in `op://` URIs — must use item UUID

### Gemini working via wrapper
- `~/.local/bin/gemini-with-op` — wrapper script that injects key from 1Password
- `~/.gemini/settings.json` set to `{"security": {"auth": {"selectedType": "gemini-api-key"}}}`
- Confirmed working: `gemini-with-op 'Reply with only: OK'` → returns `OK`

### OpenClaw native Gemini provider (in progress)
- Added `models.providers.google` to `openclaw.json` with `api: "google-generative-ai"`
- Added `secrets.providers.onepassword_gemini` exec provider pointing to `op read`
- **Problem**: Gateway systemd service doesn't have `OP_SERVICE_ACCOUNT_TOKEN` in its environment
- Attempted systemd drop-in at `~/.config/systemd/user/openclaw-gateway.service.d/secrets.conf` — command got SIGTERM before completing
- **Status**: Incomplete — need to finish systemd drop-in + restart gateway + verify models scan

### Phase 2 spec
- Written to `docs/PHASE2-SPEC.md` and Obsidian vault
- My pre-review: solid spec, minor flag on TBD gateway API paths
- Waiting for Bank's approval before building
- Bank has not yet reviewed/approved

## Key decisions today
- OpenClaw native Gemini provider preferred over CLI wrapper (better integration)
- `~/.config/secrets/` as canonical secrets directory for nuc-13-pro
- 1Password service account is the right auth method for headless servers
- tmux socket approach works but requires `exec bash` (default shell is `sh` which doesn't support process substitution)
