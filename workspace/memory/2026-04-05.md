# 2026-04-05 — Daily Notes

- Bank prefers Alice to handle sub-agents behind the scenes and report back directly in the same chat.
- Brief status updates are enough when delegating; no need to explain why each time.
- Do not switch the conversation voice to another agent unless explicitly desired.
- If Bank explicitly asks to talk to another agent, clearly announce that the switch is happening first.
- Hard rule for Telegram: only Alice may speak directly to Bank; sub-agents must remain internal and Alice reports back.
- For detailed sub-agent docs/proposals, Bank wants Alice to automate a draft -> review -> revise loop, capped at 2 review/revision rounds, then report the result.
- If Gemini CLI is working, switch the reviewer preference to Gemini 3.
- Fallback reviewer order: Codex first, then Bank.
- Role update: Codex is primarily Runner. Default coding agent should be Gemini 3, with Claude Code Sonnet as fallback.
- Bank expects Gemini to be used heavily for coding when usable because quota is available there; fallback only when Gemini is actually unhealthy.
- After each ACP task, Alice should verify cleanup/closure so stale ACP sessions do not fill the session pool.
- Bank wants updates when work is assigned or progress changes.
- If Bank says he is going to sleep, hold proactive updates until he says he is back, then send one summary of queued updates.
- Every time Bank gives a task, immediately provide a short acknowledgment (e.g., "Noted, I will do it").
- Distinguish clearly between questions and instructions. If Bank asks a question (e.g., ending with '?'), provide the answer/analysis first. Do not take action or modify files based on a question unless explicitly told to do so.
- Bank established a new **Runner** role (using `codex`).
  - Runner handles chores: config adjustments, cron jobs, permissions, and miscellaneous system work.
  - Alice (main agent) delegates these tasks to Runner to remain responsive at all times.
- Consulted Claude Code (Tech Lead) regarding Paperclip vs Mission Control Phase 1.
  - Claude's conditional advice: **Adopt Paperclip** since the repository is actually public, open-source, and auditable.
  - Sunk cost vs Value: While Mission Control Phase 1 & 2 are done, Paperclip provides native infrastructure for task queues, agent scheduling, heartbeats, approval gates, and WebSocket connectivity (openclaw gateway + devices approve). Building this custom is undifferentiated plumbing.
  - Security: The repository is public, allowing us to audit the system managing agent budgets and identity keys.
  - Workflow: We will adopt Paperclip instead of building Phase 3 of Mission Control from scratch.
- Reviewed `Paperclip Pilot — Agent Monitoring Proposal.md` and wrote a decision-oriented review note in the Mission Control PKM folder.
  - Recommendation: **approve the local Paperclip pilot path with edits**, not as execution-ready yet.
  - Key change requested: add a short Phase 0 technical fit check before the 1-2 week pilot, plus measurable success criteria, explicit pilot evidence, and clearer ownership.
- Reviewed the revised `Paperclip Pilot — Agent Monitoring Proposal.md` and updated the review note.
  - Prior feedback was materially addressed: Phase 0 fit check, tighter success criteria, required scenarios/evidence, clearer ownership, security posture, fallback, and K3s gates were all added.
  - Updated recommendation: **approve as-is for Phase 0 and Phase A**.
  - Explicitly kept **Phase C / K3s** blocked behind the proposal's stated decision gates and the unresolved Mission Control gateway/auth reliability issue.
- Converted `~/.openclaw/workspace` out of its nested git repo and into the parent `~/.openclaw` repo for the files that were already versioned there.
  - Preserved the nested repo history at `~/.openclaw/.repo-backups/workspace.git.20260405-220539` before switching tracking to the parent repo.
