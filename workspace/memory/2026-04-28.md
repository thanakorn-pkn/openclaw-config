# 2026-04-28 — Heartbeat Check

- Re-checked the Mission Control / Paperclip pilot state.
- `http://127.0.0.1:8000/api/health` returns OK (`mission-control`).
- `paperclip-dev-once` is healthy at `http://127.0.0.1:3100/api/health`.
- `paperclip-dev-once` still reports `restartRequired=true` (`reason: backend_changes`).
- `mc-pod.service` is inactive on this host; `paperclip-app.service` is inactive.
- No blocker surfaced on this check.
- Live verification at 2026-04-28 03:35 Asia/Bangkok matched the above state.

- Follow-up at 09:37 Bangkok: `paperclip-dev-once` had gone stale/offline, so I stopped the registered dev process and relaunched `pnpm dev:once`.
- Current live state: `http://127.0.0.1:3100/api/health` returns OK, `/board` returns 200, and the dev-server status now reports `restartRequired=false`.
- Live recheck at 10:38 Bangkok confirmed the same healthy state.
- Live recheck at 12:36 Bangkok confirmed the same healthy state: Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, and both `mc-pod.service` and `paperclip-app.service` remain inactive.
- Live recheck at 14:35 Bangkok confirmed the same healthy state: Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, and both `mc-pod.service` and `paperclip-app.service` remain inactive.
- Host hardening update: `hermes-gateway.service` now has `MemoryMax=512M` and was restarted successfully; current cgroup memory is capped at 512M.
- Host hardening update: added an `/etc/systemd/system/apt-daily.timer.d/override.conf` to pin apt-daily to `04:00` with `RandomizedDelaySec=30m`.
- Live recheck at 16:37 Bangkok confirmed the same healthy state: Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, and both `mc-pod.service` and `paperclip-app.service` remain inactive.
- Live recheck at 17:35 Bangkok confirmed the same healthy state: Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, `hermes-gateway.service` remains inactive, and the apt-daily timer override is present.
- Live recheck at 18:36 Bangkok confirmed Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, and `mc-pod.service`, `paperclip-app.service`, and `hermes-gateway.service` are inactive.
- Live recheck at 20:35 Bangkok confirmed Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, `mc-pod.service` is inactive, and `paperclip-app.service` is inactive.
- Live recheck at 21:35 Bangkok confirmed the same healthy state: Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, and both `mc-pod.service` and `paperclip-app.service` are inactive.
- Live recheck at 22:35 Bangkok confirmed Mission Control health is OK, `paperclip-dev-once` is healthy with `restartRequired=false`, `mc-pod.service` is active, `paperclip-app.service` is inactive, `hermes-gateway.service` is active, and `openclaw-gateway.service` is active.
- Local config check: `/etc/rancher/k3s/config.yaml` already contains `system-reserved=memory=2Gi`, `kube-reserved=memory=1Gi`, and `eviction-hard=memory.available<500Mi`.
- Local config check: `hermes-gateway.service` has `MemoryMax=512M`, `openclaw-gateway.service` has `MemoryMax=2G`, and `paperclip-db.service` has `MemoryMax=256M`.
- Local config check: `apt-daily.timer` override is present and pins the timer to `04:00` with `RandomizedDelaySec=30m`.
- Live recheck at 23:35 Bangkok confirmed Mission Control health is OK, `paperclip-dev-once` is healthy, `/board` returns 200, `restartRequired=false`, and `mc-pod.service`, `paperclip-app.service`, and `hermes-gateway.service` are inactive.
