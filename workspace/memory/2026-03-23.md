# 2026-03-23

## Mission Control — Linear Redesign + Feature Development

### Infrastructure Note
- `homelab-infra` not available on nuc-13-pro → Bank handling infra tasks himself for now

### Linear-Inspired Redesign
- Bank requested Mission Control to look like Linear
- Applied full redesign via Claude Code sub-agent:
  - Dark theme with violet accents (global CSS)
  - Sidebar redesigned with Linear styling
  - Dashboard: compact stats layout + task list
  - Board: richer task cards with description, priority, project tags, dates
  - Gateway and Projects pages updated with Linear aesthetic
  - Full-height viewport board columns with independent scrolling

### Features Implemented
- **Clickable task cards** with slide-out detail panel
- **Definition of Done (DoD)** field — editable textarea, auto-save on blur
- **Checklist** — toggle checkboxes, add/delete items via Enter or + button, auto-persist
- **Progress bar** — tied to checklist completion %, `h-0.5` height
- **Editable description** — auto-save on blur (was read-only `<p>` tag before)
- **Project names** displayed instead of numeric IDs across dashboard and board
- DB migration: added `definition_of_done` (text) + `checklist` (JSON) columns to Task model

### Remote Dev Setup
- Claude Code installed on nuc-13-pro via Termius port-forwarding for remote development
- Claude Code v2.1.81 confirmed operational

### End-of-Session Discussion
- Design validation: Is DoD as a separate field appropriate for card workflows? — unresolved
- Checklist feature implementation accepted, further refinement TBD
