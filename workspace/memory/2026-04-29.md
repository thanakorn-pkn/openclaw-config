# 2026-04-29 — Heartbeat Check

- Rechecked Mission Control / Paperclip live state just after midnight Bangkok.
- `http://127.0.0.1:8000/api/health` returns OK (`mission-control`).
- `http://127.0.0.1:3100/api/health` returns OK and reports `restartRequired=false`.
- `http://127.0.0.1:3100/board` is reachable.
- `paperclip-app.service` is inactive; `hermes-gateway.service` and `openclaw-gateway.service` are active.
- No blocker surfaced.

- Follow-up at 01:35 Bangkok: live verification still looks good — Mission Control health is OK, `paperclip-dev-once` reports `restartRequired=false`, `http://127.0.0.1:3100/board` is reachable, `mc-pod.service` is active, and `paperclip-app.service` remains inactive.
- Recheck at 02:35 Bangkok: Mission Control health is still OK, Paperclip health is OK with `restartRequired=false`, `/board` still returns 200, and `openclaw-gateway.service`, `hermes-gateway.service`, `mc-pod.service`, and `paperclip-app.service` are all inactive/expected.
- Live recheck at 03:35 Bangkok confirms Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are inactive.
- Fresh 04:35 Bangkok verification: `mission-control` health is OK, `paperclip-dev-once` still reports `restartRequired=false`, `/board` is reachable, and all four checked services remain inactive as expected.
- Local hardening check: `systemctl --user show` confirms `MemoryMax=512M` for `hermes-gateway.service`, `MemoryMax=2G` for `openclaw-gateway.service`, and the Paperclip DB quadlet under `~/.config/containers/systemd/paperclip-db.container` carries `MemoryMax=256M`.
- Latest live pass: `http://127.0.0.1:8000/api/health` is OK, `http://127.0.0.1:3100/api/health` is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are active while `paperclip-app.service` remains inactive/expected.
- Fresh check at 06:35 Bangkok matches the same state.
- Fresh check at 07:36 Bangkok: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, `mc-pod.service`/`hermes-gateway.service`/`openclaw-gateway.service` are active, and `paperclip-app.service` remains inactive.
- 08:37 Bangkok fix: `paperclip-app.service` had drifted to a stale pnpm path (`~/.local/share/pnpm/pnpm`) and was failing with `203/EXEC`; patched the unit to use `/data/pnpm/pnpm`, reloaded systemd, restarted the service, and verified `http://127.0.0.1:3100/api/health` is OK with `restartRequired=false` and `/board` returns 200.
- 10:35 Bangkok verification: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.

- OpenClaw self-update check: installed 2026.4.26 (be8c246), latest published 2026.4.26; no update/restart needed.
- 11:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.
- 12:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.
- 14:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` remain active.
- 15:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` remain active.
- 16:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` remain active.
- 17:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.
- 18:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.

- 19:36 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.
- 20:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.
- 21:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.
- 23:35 Bangkok heartbeat check: Mission Control health is OK, Paperclip health is OK with `restartRequired=false`, `/board` returns 200, and `mc-pod.service`, `paperclip-app.service`, `hermes-gateway.service`, and `openclaw-gateway.service` are all active.
