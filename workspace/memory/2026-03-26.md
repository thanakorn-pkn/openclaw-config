# 2026-03-26

## Morning: OpenClaw Config Issues + Option B Confirmed

### Session `dda12bf4` (07:11–09:05 Bangkok)
- `skip_design` clarification: fast-track flag that bypasses Design → Review columns
- **Option B confirmed** (was agreed on 2026-03-25 but never written to memory):
  - Drop `skip_design` entirely — everything goes through full 8-col flow
  - Bug fixes / small tasks use a one-liner spec (e.g. "Fix: search crashes on empty query")
  - Bank still reviews even tiny specs; overhead tiny, consistency high
- User discovered chat history gone — daily 4 AM session reset was wiping context
- OpenClaw doctor findings:
  - Gateway entrypoint mismatch after pnpm global update (pnpm versioned path vs direct path)
  - Fix: `openclaw doctor --fix` + `openclaw gateway restart`
  - Telegram plugin: failed (unknown error at the time)
  - Memory search: disabled — no embedding provider API keys configured

## Session `36c860d3` (09:05 AM → ongoing)

### Root Cause & Fixes Applied
- **Root cause**: `session.reset.mode: "daily"` → reset at 4 AM Bangkok every day
- **Fix 1**: Changed to `session.reset.mode: "idle"`, `idleMinutes: 10080` (7 days) — no more daily wipes
- **Fix 2**: `session.dmScope` → `"main"` — Telegram DMs and webchat now share one session
- **Fix 3**: Nightly cron added (ID: `11d42f7e-ccab-4699-a2e4-72c8ea1d3c3f`):
  - Schedule: `50 20 * * *` UTC = 3:50 AM Bangkok
  - Task: summarize today's conversation → `memory/YYYY-MM-DD.md` + update `MEMORY.md`
- **Fix 4**: Added "Write to Daily Note Mid-Session" rule to AGENTS.md as backup to cron

### OpenClaw Update
- Web UI "Update Software" button broken (OpenClaw installed via pnpm, not git — button does git pull)
- Used `openclaw update` CLI instead → updated to version 2026.3.24

### Memory Backfill Task
- Raw JSONL session archives identified and read for historical context
- Creating missing daily notes for 2026-03-22, 2026-03-23, 2026-03-26
- Updating 2026-03-25 with Gemini double-duty details
- Fixing MEMORY.md errors (Board Flow, task count, Reviewer description)

## Evening: Mission Control Phase 1 Build (18:30–21:00+)

### Spec Updates (18:30)
- Reviewed Phase 1 spec in both Obsidian vault and repo
- Applied **Option B** across all spec files:
  - Removed `skip_design` field from data model, API, Alembic migration, drag & drop, polish task
  - Removed fast-track flow from board diagram
  - Updated Gemini reviewer model: `gemini-2.5-pro` → `default model` (2.5-pro returns 404)
- Both files now in sync and committed

### Phase 1 Build — Tasks Completed
Sequential build via Claude Code sub-agents (ACP), one task at a time:

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | DB migration | ✅ Done | New enums, assignees/task_relationships/task_activities tables, sprints dropped |
| 2 | Assignees CRUD | ✅ Done | GET/POST/PATCH/DELETE /api/assignees, 5 defaults seeded |
| 3 | Issue IDs | ✅ Done | Project prefix, task number, issue_id in responses (MC-1) |
| 4 | Activity logging | ✅ Done | service + wired into all mutations, GET /api/tasks/{id}/activities |
| 5 | Dependencies | ✅ Done | blocks/relates_to, 409 block enforcement on in_progress |
| 6 | Task detail revamp | ✅ Done | is_blocked computed, comment endpoint, all fields in PATCH |
| 7 | Markdown support | ✅ Done | markdown-editor.tsx + markdown-viewer.tsx, @tailwindcss/typography |
| 8 | Date pickers | ✅ Done | calendar.tsx + date-picker.tsx using react-day-picker |
| 9 | Drag & drop board | 🔄 Running | @dnd-kit, 8 columns, task-card.tsx |
| 10–14 | Review flow, Search, Filtering, Dashboard, Polish | ⏳ Queued | |

### Key Technical Decisions Made Today
- TaskRelationship.type uses `native_enum=False` (VARCHAR not PG enum) — avoids cast errors
- All task endpoints use `selectinload(Task.project)` for issue_id availability
- is_blocked computed on GET/PATCH responses via `_compute_is_blocked()` async helper
- Activity logging fires only when values actually change (not on every PATCH)
- Build script at `scripts/build-phase1.sh` chains Tasks 6–14 sequentially

### Workflow Confirmed
- Bank's preference: I (coordinator/main agent) handle planning, routing, notifications
- Claude Code sub-agents do all implementation
- Notify Bank after each task completes
